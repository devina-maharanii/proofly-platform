/** Evidence Ledger Editorial — Phase 32 loading state names the private, source-checking boundary rather than implying an opaque ranking operation. */
export default function TalentMatchingLoading() {
  return (
    <main className="matching-page" aria-busy="true" aria-live="polite">
      <header className="matching-hero">
        <p className="matching-kicker">Private matching view</p>
        <h1>Checking current proof and project sources.</h1>
        <p>
          Recommendations are revalidated against current consent, visibility,
          proof, and project requirements before they are shown.
        </p>
      </header>
    </main>
  );
}
